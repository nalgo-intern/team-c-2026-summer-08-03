# レシピ提案アプリ 依存パッケージ
#
#   インストール: pip install -r requirements.txt
#
# バージョンは macOS(ARM) / Python 3.13 での動作確認値。
# pip freeze の出力ではなく、直接使うものだけを手書きしている。

# --- Web画面 ---
streamlit==1.61.0

# --- 食材画像の認識 (CLIP ViT-B/32) ---
# torch は OS ごとに配布ビルドが異なり、固定すると Windows で入らないことが
# あるためバージョンを指定しない。
# 揃える必要が出たら torch==2.12.0 / torchvision==0.27.0 を指定する。
torch
torchvision
transformers==5.14.1

# --- データ読み込み / レシピ推薦 (TF-IDF) ---
pandas==3.0.2
scikit-learn==1.8.0
numpy

# --- 画像処理 ---
pillow==12.2.0
